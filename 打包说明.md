# 桌面端与授权签发工具 · 打包说明

## 一、准备环境（只需一次性安装）

### python安装程序

| 依赖 | 版本 | 安装方式 |
| --- | --- | --- |
| Windows | 10 / 11 | 桌面端只能在 Windows 上打包 |
| Python | **3.12（推荐）** | 官网下载安装，安装时**务必勾选 Add Python to PATH**。注意：numpy/scipy 暂不提供 3.13+ 的预编译包，请勿使用 3.13/3.14 |
| Node.js | 18 (LTS)+ | 官网下载 LTS 版安装（默认加入 PATH） |

> 若你已装了更高版本的 Python（如 3.14），无需卸载——额外安装一个 3.12 即可，打包脚本会自动优先选用 3.12 创建虚拟环境。
> 安装完 Python / Node 后，请**重新打开一个 PowerShell 窗口**，再执行下面的命令（让 PATH 生效）。

### uv版本管理工具

uv venv --python 3.12
uv pip install -r requirements.txt
uv pip install -r requirements-dev.txt

## 二、一键打包

解压源码包，在解压出的项目根目录（含 `package-desktop.ps1` 的目录）打开 PowerShell，执行：

```powershell
# 打包桌面安装包 + 授权签发工具（推荐）
.\package-all.ps1

# 或者分开执行：
.\package-desktop.ps1        # 只打包桌面安装包
.\package-sign-tool.ps1      # 只打包管理员授权签发工具
```

**首次运行会自动完成以下准备（无需手动操作）：**

1. 检查 Python / Node 是否安装（缺失会明确提示如何安装）；
2. 在项目内创建独立虚拟环境 `.venv`（不污染系统 Python）；
3. 在 `.venv` 中安装后端依赖与打包工具（PyInstaller）；
4. 为前端 / 桌面壳安装 npm 依赖；
5. 从随包附带的 `scripts\keys` 生成内嵌私钥的签发工具（不会改动密钥）。

> 首次打包需要联网下载依赖与 electron 构建资源，耗时较长，请耐心等待；之后再次打包会自动跳过已完成的步骤。

## 三、产出物

| 内容 | 路径 |
| --- | --- |
| 桌面安装包（发给最终用户安装） | `release\desktop\windows\wtcmd-platform-desktop-*-win-x64.exe` |
| 管理员授权签发工具 | `release\license-admin\windows\wtcmd-license-admin.exe` |

## 四、授权签发工具使用（管理员）

- `wtcmd-license-admin.exe` 是带图形界面的授权管理程序：管理被授权设备信息，并为设备指纹签发授权码。
- 双击运行，按界面提示：读取设备码 → 录入使用人/有效期 → 签发授权码 → 交付给对应设备激活。
- **安全须知：** 该工具**内嵌授权私钥**，任何持有者都能签发合法授权码。仅限管理员在受控机器上使用，**切勿随桌面安装包一起分发给最终用户**。

## 五、桌面端使用与激活（最终用户）

1. 运行安装包，按向导安装。
2. 首次启动为 3 天试用；试用到期后进入激活页。
3. 用户将界面显示的**设备码**提供给管理员。
4. 管理员用签发工具生成**授权码**回传，用户在激活页粘贴授权码完成激活。

## 六、常见问题

| 现象 | 处理 |
| --- | --- |
| 提示未检测到 Python | 安装 Python 3.11+ 并勾选 Add to PATH，重开 PowerShell 再试 |
| 提示未检测到 Node.js | 安装 Node.js 18+，重开 PowerShell 再试 |
| 依赖下载缓慢 / 失败 | 检查网络；可配置 npm / pip 国内镜像源后重试 |
| 想清理重来 | 删除项目内的 `.venv`、`frontend\node_modules`、`desktop\node_modules`、`release\` 后重新运行脚本 |
| 找不到签发工具私钥 | 确认解压时保留了 `scripts\keys` 目录（随源码包一并交付） |
