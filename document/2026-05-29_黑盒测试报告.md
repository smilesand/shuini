# 水泥配比计算器黑盒测试报告

测试日期：2026-05-29

## 1. 测试范围

本次测试覆盖以下范围：

- 后端 API 黑盒测试
- 前端关键页面 UI 冒烟测试
- 边界值测试
- 基础安全测试
- 本地开发环境性能测试

本次测试使用的主要入口：

- 后端服务：`http://127.0.0.1:8000`
- 前端服务：`http://127.0.0.1:5173`
- 自动化脚本：`document/run_blackbox_tests.py`

## 2. 执行方式

执行过的验证命令如下：

```powershell
Set-Location backend
python -m unittest tests.test_hpc_trial_service tests.test_record_trial_data

Set-Location ..\frontend
npm run build

Set-Location ..
python .\document\run_blackbox_tests.py
```

另外补充执行了浏览器端真实交互冒烟测试，覆盖登录、项目页、HPC 计算入口、项目选择、记录载入。

## 3. 总结结论

### 3.1 API 黑盒结果

- 总用例数：28
- 通过：28
- 失败：0

结论：当前后端 API 在本次黑盒覆盖范围内未复现功能性失败。

### 3.2 前端 UI 冒烟结果

- 登录页可正常渲染
- 使用默认管理员账户可成功登录
- 首页可正常渲染
- 项目管理页可正常加载项目列表
- 高性能混凝土计算页可进入项目选择流程
- 选择已有项目后，项目记录列表可正常联动加载
- 点击“加载”后，可成功进入主计算界面并显示历史配比数据

结论：当前前端关键主流程在桌面 Chromium 环境下可用。

## 4. 详细测试结果

### 4.1 烟雾测试

| 用例 | 预期 | 结果 |
| --- | --- | --- |
| GET `/` | 返回 200 和 docs 入口 | 通过 |
| 前端未登录访问 `/` | 重定向到登录页 | 通过 |
| 登录后进入首页 | 首页内容正常渲染 | 通过 |
| 进入项目管理页 | 列表正常加载 | 通过 |
| 进入 `/calc/hpc` | 页面可渲染项目选择区域 | 通过 |

### 4.2 业务流程测试

| 用例 | 预期 | 结果 |
| --- | --- | --- |
| 管理员登录 | 返回 token、用户名、角色信息 | 通过 |
| 管理员查询个人资料 | 返回当前用户资料 | 通过 |
| 管理员查询用户列表 | 返回用户集合 | 通过 |
| 管理员创建普通用户 | 创建成功 | 通过 |
| 普通用户登录 | 创建后可用默认密码登录 | 通过 |
| 管理员创建项目 | 创建成功 | 通过 |
| 创建重复项目编号 | 被拒绝 | 通过 |
| 管理员创建记录 | 创建成功 | 通过 |
| 项目记录列表查询 | 返回刚创建记录 | 通过 |
| 测试数据清理 | 用户、项目、记录均可删除 | 通过 |

### 4.3 边界值测试

| 接口 | 测试数据 | 预期 | 结果 |
| --- | --- | --- | --- |
| POST `/api/water-binder` | 正常值 `fcuk=60, fb=48` | 正常返回 | 通过 |
| POST `/api/water-binder` | `fcuk=0` | 422 拒绝 | 通过 |
| POST `/api/aggregate` | `beta_s=0.9999` | 可计算 | 通过 |
| POST `/api/aggregate` | `beta_s=1` | 422 拒绝 | 通过 |
| POST `/api/water-admixture` | `alpha=100` | 可计算 | 通过 |
| POST `/api/water-admixture` | `alpha=100.01` | 422 拒绝 | 通过 |
| POST `/api/hpc-trial` | 正常试配参数 | 正常返回多组结果 | 通过 |
| POST `/api/hpc-trial` | `batch_volume=0` | 422 拒绝 | 通过 |

### 4.4 安全测试

| 用例 | 预期 | 结果 |
| --- | --- | --- |
| 未登录访问 `/api/projects` | 401 | 通过 |
| 错误密码登录 | 401 | 通过 |
| 篡改 token 访问 `/api/auth/profile` | 401 | 通过 |
| 普通用户访问 `/api/auth/users` | 403 | 通过 |
| 使用 `"' OR 1=1 --"` 作为项目搜索词 | 查询稳定返回，不击穿 | 通过 |
| 上传 `.txt` 到 `/api/upload-fit` | 400 拒绝 | 通过 |
| 上传大于 5 MB 的 CSV | 400 拒绝 | 通过 |

说明：本次安全测试属于轻量级黑盒校验，不等同于完整渗透测试、依赖漏洞扫描或生产级 DAST。

### 4.5 性能测试

#### 4.5.1 `/api/water-binder`

- 并发数：6
- 总请求数：60
- 成功：60
- 失败：0
- 平均响应时间：9.47 ms
- P95：27.10 ms
- 最大值：28.30 ms
- 总墙钟时间：105.32 ms

#### 4.5.2 `/api/hpc-trial`

- 并发数：4
- 总请求数：24
- 成功：24
- 失败：0
- 平均响应时间：6.97 ms
- P95：19.14 ms
- 最大值：19.49 ms
- 总墙钟时间：47.05 ms

说明：以上性能结果来自本机开发环境，仅可作为本地回归基线，不能替代生产部署后的容量评估。

## 5. UI 冒烟记录

### 5.1 登录流程

- 打开前端首页后，未登录用户被重定向到 `/login`
- 输入 `admin / 123456` 后可成功登录
- 登录后首页可正常显示系统主导航与欢迎内容

### 5.2 项目页

- `/projects` 页面成功加载
- 页面可见“新建项目”“搜索”等关键操作项
- 本地数据中已有项目可正常渲染

### 5.3 HPC 计算入口

- `/calc/hpc` 页面可正常打开
- 选择已有项目后，URL 正常附带 `project_id`
- 当前项目记录列表可联动加载
- 点击“加载”后可进入带历史数据的计算界面
- 水胶比、砂率、骨料、胶凝材料、水和外加剂等主标签区可正常显示

## 6. 本次测试未发现的明显功能故障

在本次覆盖范围内，未复现以下类型的显著故障：

- 登录失败后状态异常
- 普通用户越权访问管理员接口
- 试配接口边界值导致 500
- 文件上传类型和大小校验失效
- 项目与记录的基本 CRUD 失效
- 前端主流程因接口对接问题而中断

## 7. 已知限制与未覆盖项

以下内容未纳入本轮黑盒测试，建议后续继续补齐：

- 多浏览器兼容性测试
- 移动端适配测试
- 长时间稳定性与 soak test
- 高频并发下的真实压测
- 登录暴力破解对抗验证
- 依赖漏洞扫描与供应链安全检查
- 前端异常监控与浏览器控制台错误采集归档

## 8. 结论

从黑盒测试角度看，项目当前已经具备可运行、可登录、可查询、可计算、可试配、可上传、可做基础权限控制的能力，主流程可用。

后续优化重点不在“已有功能是否能跑起来”，而在以下方面：

- 安全基线加固
- 架构拆分与职责收敛
- 工程规范与自动化测试补齐
- 前端包体与加载性能优化
- 文档与注释的一致性提升

具体修改建议见 `document/2026-05-29_项目优化建议.md`。